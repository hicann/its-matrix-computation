# Security Policy

## 1. 安全范围

SimNPU 是面向 NPU 矩阵计算的本地性能仿真工具，主要读取本地 Python 源码、JSON 配置、CSV 效率曲线和矩阵维度数据，并在本地执行仿真与搜索。

本安全声明覆盖：

- SimNPU 源代码；
- 依赖安装和运行环境；
- 硬件配置及数据文件；
- 命令行输入；
- 测试脚本和测试结果；
- 仓库中提供的文档、示例和辅助脚本。

本声明不替代操作系统、Python 环境、软件源或宿主机自身的安全策略。

## 2. 支持版本

安全修复原则上优先应用于当前主分支和最新维护版本。

| 版本 | 安全支持状态 |
|---|---|
| 当前主分支 | 支持 |
| 最新正式版本 | 支持 |
| 历史版本 | 视维护情况提供必要修复 |
| 非官方分支或第三方修改版本 | 不保证 |

如仓库发布策略发生变化，应同步更新本表。

## 3. 运行用户建议

### 3.1 禁止不必要的高权限运行

SimNPU 正常运行不需要 root 权限，也不需要直接控制实际 NPU 设备。

建议：

- 使用普通用户运行；
- 在 Python 虚拟环境或隔离环境中安装依赖；
- 不使用 `sudo pip install`；
- 不在系统核心目录中运行项目；
- 不授予项目目录不必要的写权限；
- 不以具有生产环境管理权限的账号执行测试脚本；
- 不在同一环境中混放生产密钥、云平台凭据或内部数据。

示例：

```bash
python -m venv .venv
source .venv/bin/activate
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

### 3.2 输入文件来源

仅使用可信来源的：

- Python 源码；
- JSON 硬件配置；
- CSV 效率曲线；
- 矩阵维度数据；
- 依赖安装文件；
- 测试脚本。

对第三方提供的脚本和数据文件，应先进行代码审查、格式检查和恶意代码扫描。

## 4. 文件权限控制

推荐的文件和目录权限如下：

| 文件或目录 | 建议权限 | 说明 |
|---|---:|---|
| 仓库目录 | `0755` | 所有者可写，其他用户只读和进入 |
| `src/`、`data/`、`image/` | `0755` | 避免非所有者修改 |
| Python 源码 | `0644` | 仅所有者可写 |
| JSON、CSV、Markdown 文件 | `0644` | 仅所有者可写 |
| 确需直接执行的脚本 | `0755` | 仅对必要脚本增加执行权限 |
| 本地敏感配置 | `0600` | 不应提交到仓库 |
| 私钥和凭据文件 | `0600` | 不应存放在项目目录或提交到仓库 |
| 测试输出和日志 | `0640` 或更严格 | 根据内容控制可见范围 |

设置示例：

```bash
chmod 755 .
find src data image -type d -exec chmod 755 {} \;
find src data image -type f -exec chmod 644 {} \;
```

只有确需直接执行的脚本才应设置执行权限：

```bash
chmod 755 path/to/script.sh
```

不要对仓库目录执行以下操作：

```bash
chmod -R 777 .
```

## 5. 构建与安装安全声明

### 5.1 依赖安装

安装依赖前应：

- 检查 `requirements.txt` 的修改记录；
- 使用可信的软件源；
- 避免使用来源不明的镜像；
- 在隔离环境中安装；
- 定期检查依赖安全漏洞；
- 对新增依赖说明用途和许可证；
- 避免无版本约束地引入高风险依赖。

推荐命令：

```bash
python -m pip install -r requirements.txt
```

如组织规定使用内部软件源，应按照组织安全策略配置，不应将内部账号、Token 或私有软件源凭据写入仓库。

### 5.2 源码检查

运行前建议执行：

```bash
python -m py_compile src/*.py
```

如项目提供自动化安全扫描，应同时执行依赖扫描、静态代码检查和敏感信息扫描。

### 5.3 构建脚本约束

新增或修改构建、安装、启动脚本时：

- 不得静默下载和执行远程脚本；
- 不得修改与项目无关的系统配置；
- 不得关闭防火墙或系统安全机制；
- 不得扩大文件权限；
- 不得写入系统敏感目录；
- 不得记录明文密码、Token 或密钥；
- 不得拼接未经校验的外部输入执行 Shell 命令；
- 应明确列出需要的环境变量和目录权限。

## 6. 运行安全声明

### 6.1 本地执行边界

SimNPU 默认作为本地仿真工具运行。正常运行过程中：

- 不要求访问公网；
- 不要求开放网络端口；
- 不启动对外网络服务；
- 不需要实际 NPU 设备权限；
- 不需要访问用户主目录之外的敏感文件；
- 不应上传输入数据、配置或结果；
- 不应收集遥测或用户隐私信息。

如后续版本引入网络服务、自动下载、遥测、远程 API 或云端功能，必须在代码合入前完成安全评审，并同步更新本文件和 README。

### 6.2 输入校验

对以下输入应进行合法性检查：

- 矩阵维度；
- 搜索模式；
- 硬件配置字段；
- 存储容量；
- 带宽和效率数值；
- CSV 列名及数据类型；
- 文件路径；
- 搜索次数和搜索边界。

应拒绝或明确提示：

- 非法、缺失或负数维度；
- 超出合理范围的配置值；
- 缺失字段；
- 无法解析的数据；
- 不存在或无权限访问的文件；
- 可能造成极端资源消耗的输入。

### 6.3 资源使用

`bayes` 和 `exhaustive` 搜索可能消耗较多 CPU 时间和内存。建议：

- 在非生产环境运行高开销测试；
- 对输入规模和搜索空间设置合理上限；
- 运行前确认可用内存和磁盘空间；
- 避免多个高开销任务无控制并发；
- 对长时间任务保留日志并设置合理超时；
- 避免将未经验证的搜索任务放入共享生产节点。

## 7. 公网地址声明表

当前 SimNPU 设计不依赖固定公网地址。

| 使用场景 | 公网地址或端口 | 是否为运行必需 | 说明 |
|---|---|---|---|
| SimNPU 正常运行 | 无 | 否 | 本地读取配置和数据并执行仿真 |
| 对外监听端口 | 无 | 否 | 默认不启动网络服务 |
| 遥测或数据上报 | 无 | 否 | 默认不收集或上传用户数据 |
| 模型或数据自动下载 | 无 | 否 | 当前不要求运行时自动下载 |
| Python 依赖安装 | 由用户配置的软件源决定 | 否 | 仅安装阶段可能访问软件源，仓库不应硬编码账号或私有地址 |
| 远程 API | 无 | 否 | 当前不调用远程推理或云端 API |

若后续新增公网域名、IP 地址、端口、远程 API 或自动下载功能，应在合入前：

1. 在本表中登记；
2. 说明用途、数据范围和必要性；
3. 评估传输加密、身份认证和访问控制；
4. 更新 README 和部署文档；
5. 完成安全检视。

## 8. 敏感信息管理

禁止向仓库提交：

- 用户名和密码；
- API Token；
- 私钥和证书私钥；
- 云平台访问凭据；
- 内部域名和内部 IP；
- 个人隐私信息；
- 未脱敏的内部日志；
- 软件源认证信息；
- 生产环境配置；
- 含敏感目录信息的调试文件。

建议在提交前检查：

```bash
git diff --cached
git status
```

本地敏感文件应加入 `.gitignore`，但 `.gitignore` 不能替代敏感信息扫描和人工检查。

若敏感信息已经提交：

1. 立即撤销或轮换相关凭据；
2. 联系维护者；
3. 清理 Git 历史；
4. 检查是否已被下载或使用；
5. 根据组织流程完成安全事件记录。

## 9. 漏洞报告机制

### 9.1 请勿公开披露未修复漏洞

发现潜在安全漏洞时，请不要直接在公开 Issue、PR、讨论区或测试报告中披露可被利用的完整细节。

请优先通过以下方式之一联系项目维护者：

- 仓库主页中公开的维护者联系方式；
- 所属 SIG 的私有沟通渠道；
- 代码托管平台提供的私密漏洞报告功能；
- 组织内部安全问题上报渠道。

如果无法找到私密联系方式，可先向仓库维护者发送不包含利用细节的通知，请求建立私密沟通渠道。

### 9.2 报告内容

漏洞报告建议包含：

- 受影响的版本、分支或 Commit ID；
- 问题类型；
- 影响范围；
- 复现条件；
- 最小复现步骤；
- 实际结果和预期结果；
- 风险等级建议；
- 是否存在公开利用方式；
- 临时规避措施；
- 修复建议；
- 联系方式。

不要在首次公开联系中附带真实凭据、内部数据或可能造成直接危害的完整利用代码。

### 9.3 响应流程

维护者收到报告后，应尽量按照以下流程处理：

1. 确认收到报告；
2. 判断问题是否属于本项目；
3. 评估影响版本和风险；
4. 与报告者建立私密沟通；
5. 制定修复和验证方案；
6. 发布修复；
7. 在风险可控后发布安全公告；
8. 必要时更新 `CHANGELOG.md`。

建议响应时间：

| 阶段 | 建议时间 |
|---|---|
| 确认收到 | 3 个工作日内 |
| 初步评估 | 7 个工作日内 |
| 修复计划 | 根据风险等级确定 |
| 安全公告 | 修复可用且风险可控后发布 |

实际时间可能受问题复杂度和维护资源影响。

## 10. 安全问题等级参考

| 等级 | 示例 |
|---|---|
| 严重 | 可导致任意代码执行、凭据泄露或大范围系统破坏 |
| 高 | 可导致敏感信息泄露、权限提升或稳定复现的拒绝服务 |
| 中 | 需要特定条件才能触发的资源耗尽、路径处理或输入校验问题 |
| 低 | 影响有限的信息暴露、错误配置提示或低风险依赖问题 |

最终等级应结合可利用性、影响范围和实际部署方式确定。

## 11. 第三方依赖和许可证

新增依赖前应确认：

- 来源可信；
- 许可证与项目许可证兼容；
- 维护状态正常；
- 不存在已知高危漏洞；
- 依赖确有必要；
- 不会引入不必要的网络服务、遥测或高权限要求。

第三方代码、数据、图表或配置应保留必要的来源与许可证信息。

## 12. 安全限制

SimNPU 的输出是基于配置和模型假设得到的性能仿真结果，不应直接替代：

- 实际硬件测试；
- 安全认证；
- 生产系统容量评估；
- 关键业务上线验收；
- 硬件厂商正式性能结论。

用户应结合真实硬件测量结果验证仿真精度，并对输入配置、数据来源和结果解释负责。
