# Contributing to SimNPU

感谢你对 SimNPU 的关注与贡献。SimNPU 是面向 NPU 矩阵计算的性能仿真工具，支持硬件配置加载、矩阵分块与调度建模，以及 `fast`、`bayes`、`exhaustive` 三种矩阵乘搜索模式。

为保证代码质量、测试结果可复现以及社区协作效率，请在提交 Issue 或 Pull Request（PR）前阅读本指南。

## 1. 贡献范围

欢迎以下类型的贡献：

- Bug 修复；
- 新功能或新搜索策略；
- 性能优化；
- 仿真模型、硬件参数或带宽效率模型改进；
- 新增硬件配置文件；
- 测试用例、测试数据与测试报告补充；
- 文档、示例和使用说明改进；
- 安全问题修复；
- 代码重构和工程质量提升。

对于影响仿真公式、时延模型、分块策略、搜索空间或结果精度的改动，请在 PR 中说明理论依据、实现逻辑和验证方法。

## 2. 开发与贡献流程

推荐按照以下流程提交贡献：

1. Fork 本仓库到个人账号。
2. 从最新主分支创建独立开发分支。
3. 在开发分支中完成代码、测试和文档修改。
4. 执行必要的语法检查和功能测试。
5. 提交清晰、可追溯的 Commit。
6. 将开发分支推送到个人远程仓库。
7. 向本仓库发起 PR。
8. 根据维护者或 QA-SIG 的检视意见完成修改。
9. 确认所有检视意见已闭环后，由维护者合入。

建议的分支命名方式：

```text
feature/<简短功能名称>
fix/<简短问题名称>
docs/<文档名称>
test/<测试名称>
refactor/<重构内容>
```

示例：

```text
feature/add-new-npu-profile
fix/bayes-search-boundary
docs/update-installation-guide
test/add-gemv-cases
```

请避免直接在主分支上开发或提交未经验证的修改。

## 3. Issue 创建规范

提交 Issue 前，请先搜索已有 Issue，避免重复提交。

### 3.1 Bug Issue

Bug Issue 建议包含以下信息：

- 问题标题；
- SimNPU 分支、版本或 Commit ID；
- 操作系统和 Python 版本；
- 依赖版本；
- 使用的硬件配置文件；
- 执行命令；
- 输入矩阵维度或输入数据文件；
- 实际结果；
- 预期结果；
- 完整错误信息和必要日志；
- 最小复现步骤；
- 问题出现频率；
- 是否存在临时规避方法。

推荐标题格式：

```text
[Bug] <问题简述>
```

示例：

```text
[Bug] bayes 模式在部分边界矩阵维度下异常退出
```

### 3.2 功能建议 Issue

功能建议应说明：

- 当前问题或使用场景；
- 建议增加或修改的能力；
- 预期行为；
- 对现有接口、配置、性能和兼容性的影响；
- 可选的实现思路；
- 是否愿意参与开发或测试。

推荐标题格式：

```text
[Feature] <功能简述>
```

### 3.3 文档或测试 Issue

推荐标题格式：

```text
[Docs] <文档问题简述>
[Test] <测试问题简述>
```

请勿在公开 Issue 中提交密码、Token、私钥、内部地址、个人隐私数据或其他敏感信息。安全漏洞请按照 `SECURITY.md` 中的流程报告。

## 4. Pull Request 提交规范

### 4.1 基本要求

每个 PR 应：

- 聚焦一个明确问题或一组紧密相关的改动；
- 避免夹带无关格式化或大范围无意义修改；
- 说明改动内容和改动原因；
- 关联对应 Issue；
- 说明测试环境、测试方法和测试结果；
- 对新增或修改功能补充必要测试；
- 对接口、配置、目录或行为变化同步更新文档；
- 不包含密码、Token、私钥或敏感地址；
- 不提交无关的大文件、缓存、临时文件或本地环境文件；
- 保证新增文件的许可证和第三方依赖兼容；
- 完成所有检视意见闭环。

### 4.2 PR 描述建议

PR 描述至少应包含：

```markdown
## 变更描述 / Description

说明本 PR 做了什么以及为什么需要该改动。

## 改动类型 / Change Type

- [ ] Bug 修复 / Bug Fix
- [ ] 新功能 / New Feature
- [ ] 性能优化 / Performance
- [ ] 代码重构 / Refactoring
- [ ] 文档更新 / Documentation
- [ ] 测试相关 / Test
- [ ] 其它 / Other

## 关联 Issue / Related Issues

- Closes #
- References #

## 测试信息 / Testing

说明测试环境、执行命令、输入数据和关键结果。

## 检查清单 / Checklist

- [ ] 代码符合规范
- [ ] 测试添加并通过
- [ ] 文档已更新
- [ ] 无硬编码敏感信息
- [ ] 提交信息符合规范
```

只有在 PR 合入后应自动关闭 Issue 时才使用 `Closes #编号`；仅用于说明关联关系时使用 `References #编号`。

## 5. 代码规范

### 5.1 Python 代码

- 使用清晰、可读的 Python 代码；
- 建议遵循 PEP 8；
- 函数、类、变量和模块命名应表达实际含义；
- 公共函数和复杂逻辑应提供必要注释或 Docstring；
- 避免重复代码和不必要的全局状态；
- 避免硬编码本地绝对路径；
- 文件路径应优先使用相对路径、配置项或命令行参数；
- 新增异常应提供明确的错误信息；
- 不得静默忽略可能影响仿真结果的错误；
- 不得在源码中写入账号、密码、Token、私钥或内部网络地址。

### 5.2 配置和数据文件

对 `data/` 中硬件配置、效率曲线或矩阵维度文件进行修改时，应：

- 保持格式和字段命名一致；
- 说明配置项的单位和含义；
- 标明数据来源；
- 检查数值范围和边界；
- 避免混入个人、本地或敏感数据；
- 在 PR 中说明对仿真结果的预期影响；
- 必要时提供修改前后的对比结果。

## 6. 测试要求

提交 PR 前，至少执行与改动相关的测试。

### 6.1 Python 语法检查

```bash
python -m py_compile src/*.py
```

### 6.2 安装依赖

```bash
python -m pip install -r requirements.txt
```

建议在隔离的虚拟环境中安装依赖。

### 6.3 基础功能测试

根据改动范围执行对应模式：

```bash
python src/test_new_matmul_threemode.py --mode fast
python src/test_new_matmul_threemode.py --mode bayes
python src/test_new_matmul_threemode.py --mode exhaustive
```

如命令行参数与当前代码实现不一致，应以仓库最新 `README.md` 和脚本帮助信息为准，并同步更新文档。

### 6.4 配置和数据加载测试

涉及配置或数据修改时，应至少验证：

- `data/npu_910B1.json` 或新增硬件配置可正常读取；
- 带宽效率 CSV 文件可正常读取；
- 矩阵维度文件可正常读取；
- 缺失文件、字段错误和非法数值能够得到明确提示；
- 边界矩阵维度不会导致未处理异常。

### 6.5 结果验证

涉及算法、模型或性能结果的修改时，PR 应提供：

- 测试环境；
- Commit ID；
- 执行命令；
- 输入矩阵或数据集；
- 关键输出；
- 修改前后对比；
- 精度或误差统计；
- 异常样本说明；
- 必要的日志或结果文件。

不得仅以截图代替可复现的测试命令和原始结果。

## 7. Commit 信息规范

Commit 信息应简洁说明改动类型和内容。推荐格式：

```text
<type>: <简短描述>
```

常用类型：

```text
feat      新功能
fix       Bug 修复
perf      性能优化
refactor  代码重构
docs      文档更新
test      测试相关
build     构建或依赖修改
ci        持续集成修改
chore     其它维护工作
security  安全修复
```

示例：

```text
feat: add a new NPU hardware profile
fix: handle invalid matrix dimensions
docs: update bayes search instructions
test: add boundary cases for GEMV
```

一个 Commit 应尽量对应一个清晰目的。

## 8. 交付件清单

提交功能、仿真模型、硬件配置或算子相关改动时，请根据实际范围检查以下交付件：

- [ ] 源代码；
- [ ] 配置文件；
- [ ] 必要的数据或最小示例数据；
- [ ] 单元测试或功能测试；
- [ ] 测试执行命令；
- [ ] 测试日志或结果文件；
- [ ] 精度或误差验证结果；
- [ ] 性能对比结果；
- [ ] README 或使用文档；
- [ ] 接口或配置说明；
- [ ] `CHANGELOG.md` 更新；
- [ ] 第三方依赖及许可证说明；
- [ ] 安全影响说明；
- [ ] 已知问题和限制说明。

如某项不适用，请在 PR 中标注 `N/A`，不要直接删除或忽略。

## 9. 文档要求

出现以下变化时，应同步更新文档：

- 安装方式变化；
- Python 或依赖版本变化；
- 命令行参数变化；
- 目录结构变化；
- 配置字段变化；
- 搜索模式或默认参数变化；
- 输入输出格式变化；
- 新增硬件配置；
- 安全边界变化；
- 已知限制变化。

## 10. PR 检视与合入

维护者可能从以下方面检视 PR：

- 需求和实现是否一致；
- 代码逻辑是否正确；
- 接口、数据类型和硬件支持范围是否准确；
- 是否引入兼容性问题；
- 测试是否充分且结果可复现；
- 文档是否同步；
- 是否存在敏感信息或安全风险；
- 第三方依赖及许可证是否合规；
- `CHANGELOG.md` 是否更新。

提交者应及时回复检视意见。未经确认的问题不得以删除讨论、隐藏日志或修改结论的方式规避。

## 11. 行为准则

参与社区协作时，请保持专业、尊重和建设性：

- 针对技术问题讨论，不进行人身攻击；
- 尊重不同背景和经验水平的贡献者；
- 提供可验证的事实、日志和依据；
- 对测试失败、限制和已知问题如实说明；
- 不伪造测试结果或性能数据。

感谢你为 SimNPU 做出的贡献。
